import { Link } from 'react-router-dom';
import type { Muscle } from '../types/muscle';
export default function MuscleCard({muscle}:{muscle:Muscle}){return <Link to={`/muscles/${muscle.id}`} className="group medical-card rounded-3xl border border-teal-100 bg-white p-5 transition hover:-translate-y-1 hover:border-teal-300 dark:border-slate-800 dark:bg-slate-900"><div className="text-xs font-bold uppercase tracking-wider text-teal-700 dark:text-teal-300">{muscle.region}</div><h3 className="mt-2 text-xl font-bold text-slate-900 dark:text-white">{muscle.name}</h3><p className="mt-2 line-clamp-2 text-sm text-slate-600 dark:text-slate-300">{muscle.function}</p><div className="mt-4 flex flex-wrap gap-2">{muscle.actions.slice(0,3).map(a=><span key={a} className="rounded-full bg-teal-50 px-3 py-1 text-xs text-teal-800 dark:bg-teal-900/40 dark:text-teal-200">{a}</span>)}</div></Link>}

import { Search } from 'lucide-react';
import { getActions, regions } from '../services/muscleService';
type Props={query:string;region:string;action:string;setQuery:(v:string)=>void;setRegion:(v:string)=>void;setAction:(v:string)=>void};
export default function SearchFilters({query,region,action,setQuery,setRegion,setAction}:Props){return <div className="medical-card rounded-3xl bg-white/90 p-4 dark:bg-slate-900/90"><div className="grid gap-3 md:grid-cols-[1fr_180px_220px]"><label className="relative"><Search className="absolute left-3 top-3 h-5 w-5 text-slate-400"/><input aria-label="Search muscles" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search muscles, actions, origin..." className="w-full rounded-2xl border border-slate-200 bg-white py-3 pl-10 pr-4 outline-none focus:border-teal-500 dark:border-slate-700 dark:bg-slate-950"/></label><select value={region} onChange={e=>setRegion(e.target.value)} className="rounded-2xl border border-slate-200 bg-white px-4 dark:border-slate-700 dark:bg-slate-950"><option>All</option>{regions.map(r=><option key={r}>{r}</option>)}</select><select value={action} onChange={e=>setAction(e.target.value)} className="rounded-2xl border border-slate-200 bg-white px-4 dark:border-slate-700 dark:bg-slate-950"><option>All</option>{getActions().map(a=><option key={a}>{a}</option>)}</select></div></div>}

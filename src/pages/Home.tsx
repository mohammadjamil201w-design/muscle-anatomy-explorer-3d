import { useMemo, useState } from 'react';
import { Link } from 'react-router-dom';
import SearchFilters from '../components/SearchFilters';
import MuscleCard from '../components/MuscleCard';
import { regions, searchMuscles } from '../services/muscleService';
export default function Home(){const [query,setQuery]=useState('');const [region,setRegion]=useState('All');const [action,setAction]=useState('All');const results=useMemo(()=>searchMuscles(query,region,action),[query,region,action]);return <div className="mx-auto max-w-7xl space-y-10 p-5 md:p-10"><section className="overflow-hidden rounded-[2rem] bg-gradient-to-br from-teal-700 via-cyan-700 to-slate-900 p-8 text-white medical-card md:p-14"><div className="max-w-3xl"><p className="font-semibold uppercase tracking-[.3em] text-teal-100">Medical education</p><h1 className="mt-4 text-4xl font-black md:text-6xl">Muscle Anatomy Explorer</h1><p className="mt-5 text-lg text-cyan-50">Explore the human muscular system with interactive views, concise anatomy notes, motion demos, quizzes, comparisons, and bookmarks.</p><div className="mt-8 flex flex-wrap gap-3"><Link className="rounded-2xl bg-white px-5 py-3 font-bold text-teal-800" to="/explorer">Open anatomy viewer</Link><Link className="rounded-2xl border border-white/40 px-5 py-3 font-bold" to="/quiz">Start quiz</Link></div></div></section><SearchFilters query={query} region={region} action={action} setQuery={setQuery} setRegion={setRegion} setAction={setAction}/><section><h2 className="text-2xl font-bold">Body regions</h2><div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{regions.map(r=><button key={r} onClick={()=>setRegion(r)} className="rounded-3xl border border-teal-100 bg-white p-6 text-left transition hover:-translate-y-1 hover:border-teal-400 dark:border-slate-800 dark:bg-slate-900"><p className="text-xl font-bold">{r}</p><p className="mt-2 text-sm text-slate-500">Explore muscles, actions, innervation, and clinical context.</p></button>)}</div></section><section><div className="flex items-end justify-between"><h2 className="text-2xl font-bold">Instant results</h2><span className="text-sm text-slate-500">{results.length} muscles</span></div><div className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{results.map(m=><MuscleCard key={m.id} muscle={m}/>)}</div></section></div>}

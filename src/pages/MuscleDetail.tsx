import { Link, useParams } from 'react-router-dom';
import FunctionDemo from '../components/FunctionDemo';
import Button from '../components/Button';
import { getMuscleById } from '../services/muscleService';
import { useLocalStorage } from '../hooks/useLocalStorage';
export default function MuscleDetail(){const {id}=useParams();const muscle=getMuscleById(id ?? '');const [favorites,setFavorites]=useLocalStorage<number[]>('mae-favorites',[]);if(!muscle)return <div className="p-10"><h1 className="text-3xl font-bold">Muscle not found</h1><Link to="/explorer">Return to explorer</Link></div>;const isFav=favorites.includes(muscle.id);const toggle=()=>setFavorites(isFav?favorites.filter(x=>x!==muscle.id):[...favorites,muscle.id]);const rows=[['Function',muscle.function],['Origin',muscle.origin],['Insertion',muscle.insertion],['Innervation',muscle.innervation],['Blood Supply',muscle.bloodSupply],['Clinical Importance',muscle.clinicalImportance]];return <div className="mx-auto max-w-6xl space-y-6 p-5 md:p-10"><section className="medical-card rounded-[2rem] bg-white p-8 dark:bg-slate-900"><div className="flex flex-wrap items-start justify-between gap-4"><div><p className="text-sm font-bold uppercase tracking-wider text-teal-600">{muscle.region}</p><h1 className="mt-2 text-4xl font-black">{muscle.name}</h1></div><Button onClick={toggle}>{isFav?'Remove bookmark':'Add bookmark'}</Button></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.1fr_.9fr]"><div className="space-y-3">{rows.map(([k,v])=><div key={k} className="rounded-2xl bg-slate-50 p-4 dark:bg-slate-950"><dt className="font-bold text-teal-700 dark:text-teal-300">{k}</dt><dd className="mt-1 text-slate-700 dark:text-slate-200">{v}</dd></div>)}</div><div className="rounded-3xl bg-gradient-to-br from-cyan-50 to-teal-100 p-6 dark:from-slate-800 dark:to-slate-950"><h2 className="font-bold">Common actions</h2><ul className="mt-4 space-y-2">{muscle.actions.map(a=><li key={a} className="rounded-xl bg-white/70 px-4 py-3 dark:bg-slate-900/70">{a}</li>)}</ul><div className="mt-6 rounded-2xl border border-dashed border-teal-300 p-4 text-sm text-slate-600 dark:text-slate-300">Diagram placeholder: replace <code>{muscle.image}</code> with validated medical illustrations or licensed SVG assets.</div></div></div></section><FunctionDemo muscle={muscle}/></div>}

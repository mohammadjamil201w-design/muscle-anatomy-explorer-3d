import { useState } from 'react';
import { allMuscles } from '../services/muscleService';
export default function Compare(){const [a,setA]=useState(1);const [b,setB]=useState(2);const left=allMuscles.find(m=>m.id===a)!;const right=allMuscles.find(m=>m.id===b)!;const fields=['region','function','origin','insertion','innervation','bloodSupply'] as const;return <div className="mx-auto max-w-6xl p-5 md:p-10"><h1 className="text-4xl font-black">Muscle comparison</h1><div className="mt-6 grid gap-4 md:grid-cols-2"><select value={a} onChange={e=>setA(Number(e.target.value))} className="rounded-2xl border p-3 dark:bg-slate-900">{allMuscles.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select><select value={b} onChange={e=>setB(Number(e.target.value))} className="rounded-2xl border p-3 dark:bg-slate-900">{allMuscles.map(m=><option key={m.id} value={m.id}>{m.name}</option>)}</select></div><div className="mt-6 overflow-hidden rounded-3xl bg-white dark:bg-slate-900"><table className="w-full border-collapse text-left"><tbody>{fields.map(f=><tr key={f} className="border-b border-slate-100 dark:border-slate-800"><th className="w-40 p-4 capitalize text-teal-700 dark:text-teal-300">{f.replace(/([A-Z])/g,' $1')}</th><td className="p-4">{String(left[f])}</td><td className="p-4">{String(right[f])}</td></tr>)}</tbody></table></div></div>}
